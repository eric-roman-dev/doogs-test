import { FiltreRecherchePipe } from './filtre-recherche.pipe';

describe('FiltreRecherchePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltreRecherchePipe();
    expect(pipe).toBeTruthy();
  });
});
